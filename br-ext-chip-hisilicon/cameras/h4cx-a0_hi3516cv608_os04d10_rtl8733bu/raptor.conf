# Pins that are facts about this board, merged into /etc/raptor.conf at build
# time by raptor-streaming's post-build hook. They were read off the unit's
# own configuration on 2026-09-25; see README.md for what has and has not
# been verified against them.
[ircut]
gpio_ircut = 61
gpio_ircut2 = 60
gpio_irled = 8
